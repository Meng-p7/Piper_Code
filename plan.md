# 开发计划

## 当前状态

- ✅ 运动学、轨迹规划、仿真控制器
- ✅ 视觉检测与抓取演示
- ✅ 手眼标定（批量验证 100% 通过率）
- ✅ 实时闭环控制（`run_loop` + `send_joint_velocity`）
- ✅ PBVS 视觉伺服（基于位置）
- ⬜ IBVS 视觉伺服（基于图像）
- ⬜ 运动规划与自动采集

---

## 优先级总览

| 阶段 | 名称 | 优先级 | 前置条件 | 状态 |
|:----:|------|:------:|----------|:----:|
| 四 | 视觉伺服 | P1 | 阶段三 + 手眼标定 | 🔄 PBVS 已完成，IBVS 待开始 |
| 五 | 运动规划与自动采集 | P2 | 阶段四 | ⬜ 待开始 |
| 六 | Sim2Real 迁移 | P3 | 阶段四/五 | ⬜ 待开始 |

---

## 已知问题

| # | 问题 | 严重度 | 状态 | 说明 |
|---|------|--------|:----:|------|
| 1 | 视觉检测仅支持纯色 HSV | 中 | ⬜ | 无遮挡/多目标/光照变化鲁棒性 |
| 2 | 无碰撞检测 | 中 | ⬜ | 轨迹规划开环，不检查路径安全性 |
| 3 | grasp_ball_demo 硬编码偏移量 | 低 | ⬜ | 如 `[0.02, -0.01, -0.135]` 应配置化 |

---
## 阶段四：视觉伺服 [P1]

> 前置：实时闭环控制（已完成）+ 手眼标定（已完成）
> 目标：闭环视觉导引，补偿开环误差

### 4.1 PBVS（基于位置）— ✅ 已完成

| # | 任务 | 实现文件 | 状态 |
|---|------|----------|:----:|
| 1 | 误差计算 | `PBVSController.compute_error()` | ✅ |
| 2 | 阻尼伪逆控制律 | `PBVSController.compute_joint_velocity()` | ✅ |
| 3 | 收敛判断 | `PBVSController.is_converged()` | ✅ |
| 4 | 集成抓取 | `pbvs_grasp_demo.py` 到位后触发夹爪 | ✅ |
| 5 | 演示脚本 | `demos/pbvs_grasp_demo.py` | ✅ |

**实现细节**：
- 位置误差：`e_p = p_target - p_current`
- 姿态误差：轴角表示 `e_o = axis_angle(R_target · R_current^T)`
- 控制律：`Δq = J^T (J J^T + λ²I)^-1 · [Kp·e_p, Ko·e_o]^T`
- 闭环循环：`run_pbvs_loop()` 自动运行直到收敛或超时

**测试结果**：
- 5cm 偏移收敛：1460 步（dt=0.002s ≈ 2.9s），最终误差 0.26mm
- 10cm 大偏移收敛：2170 步（≈ 4.3s），最终误差 0.22mm
- 奇异位姿阻尼有效，速度无发散

### 4.2 IBVS（基于图像）— 后续

| # | 任务 | 说明 | 预估工作量 |
|---|------|------|:--------:|
| 1 | 特征提取 | 目标色块中心像素坐标 `(u, v)` | 低 |
| 2 | 图像雅可比 | 解析计算 `L_s`（需相机内参+深度）或 Broyden 在线更新 | 高 |
| 3 | 控制律 | `Δq = pinv(L_s · J) · K · (s - s*)` | 中 |
| 4 | 收敛判断 | 图像误差 < 2 pixel 时停止 | 低 |

### 新增文件

```
core/visual_servo/
├── __init__.py
├── pbvs.py                   # ✅ PBVS 实现
├── ibvs.py                   # ⬜ IBVS 实现
├── image_jacobian.py         # ⬜ 图像雅可比矩阵
└── feature_tracker.py        # ⬜ 特征跟踪（光流/模板匹配）

demos/
└── pbvs_grasp_demo.py        # ✅ PBVS 抓取演示
```

### 验收标准

- [x] PBVS 从初始偏移 10cm 到收敛 < 5s（实测 4.3s）
- [ ] IBVS 对 ±5° 姿态扰动鲁棒
- [x] 控制频率 ≥ 50Hz（由 MuJoCo timestep 决定）
- [ ] 抓取成功率 > 90%（10 次连续测试）

---

## 阶段五：运动规划与自动采集 [P2]

> 目标：机械臂自动探索工作空间，批量采集视觉-关节数据

### 5.1 运动规划

| # | 任务 | 说明 | 预估工作量 |
|---|------|------|:--------:|
| 1 | 工作空间采样器 | 关节限位内均匀/随机采样，FK 过滤不可达位姿 | 中 |
| 2 | 碰撞检测封装 | 基于 MuJoCo `mj_collision` 封装 `is_collision(qpos)` | 中 |
| 3 | RRT* 路径规划 | 关节空间搜索无障碍路径，支持最大步长 | 高 |
| 4 | 轨迹拼接 | 路径点间用梯形速度/五次多项式平滑连接 | 中 |

### 5.2 自动数据采集

| # | 任务 | 说明 | 预估工作量 |
|---|------|------|:--------:|
| 1 | 采集编排器 | `AutoCollector`：采样位姿 → 路径规划 → 执行 → 拍照 → 存储 | 中 |
| 2 | 多视角采集 | eye-in-hand + 固定相机双视角同步 | 中 |
| 3 | 数据格式 | HDF5/NPZ，每帧含 `(image_rgb, depth, qpos, ee_pose, timestamp)` | 低 |
| 4 | 配置化 | 采集频率、数据量、存储路径从 `config.yaml` 读取 | 低 |

### 新增文件

```
core/planning/
├── __init__.py
├── workspace_sampler.py      # 工作空间采样
├── path_planner.py           # RRT* 路径规划
└── collision_checker.py      # 碰撞检测封装

core/data_collection/
└── auto_collector.py         # 自动采集编排器（data_recorder.py 已有，增强）
```

### 验收标准

- [ ] 单次采集 > 1000 帧无中断
- [ ] 路径规划成功率 > 90%（工作空间内）
- [ ] 采集数据可直接用于后续视觉伺服训练

---

## 阶段六：Sim2Real 迁移 [P3]

| # | 任务 | 说明 | 预估工作量 |
|---|------|------|:--------:|
| 1 | 真机接口实现 | 对接 Piper SDK（TCP/串口），填充 `RealRobotInterface` | 高 |
| 2 | 延迟补偿 | 通信延迟测量 + 预测补偿 | 中 |
| 3 | 仿真→真机验证 | 同一轨迹在仿真和真机上对比误差 | 中 |

---

## 技术参考

### 抓取参数

| 参数 | 值 |
|------|-----|
| `gripper_close_ctrl` | `0.015`（指间距 30mm） |
| `ball.friction` | `3.0 2.5 0.05` |
| `ball.solref` | `0.015 0.7` |
| `ball.solimp` | `0.95 0.99 0.002` |

### 坐标参考

```xml
camera: pos="0.6 0 0.5" quat="0.236 0 -0.972 0"
夹爪中心 = (data.xpos[link7] + data.xpos[link8]) / 2
freejoint qpos: [x, y, z, qw, qx, qy, qz]
```
